'use strict'

/*let a = false;
const b = true;

if (b) a = true;
if (a) console.log('yes');

//function declaration
const age1 = getAge1(1998);

function getAge1(birthYear) {
    return 2021 - birthYear;
}

// function expression

// const age2 = calcAge2(1998) it is NOT allowed to call there 🚫

const calcAge2 = function (birthYear) {
    return 2021 - birthYear;
}

const age2 = calcAge2(1998);

const calcAge3 = birthYear => 2021 - birthYear;

const age3 = calcAge3(1998);

const yearsUntilRetirement = (birthYear, firstName) => {
    const age = 2021 - birthYear;
    const retirement = 65 - age;
    return `${firstName} left ${retirement} years`;
}

const retirementYear = yearsUntilRetirement(1998, 'Bob');

// console.log(retirementYear);

function getName(name) {
    return `Hello ${name}!`;
    console.log('Is it working?')
}

const calcAverage = (a, b, c) => (a + b + c) / 3;
const avgDolphins = calcAverage(85, 54, 41);
const avgKoalas = calcAverage(123, 134, 127);

function checkWinner(avgDolphins, avgKoalas) {
    if (avgDolphins >= 2 * avgKoalas) {
        return `Dolphins win (${avgDolphins} vs ${avgKoalas})`;
    } else if (avgKoalas >= 2 * avgDolphins) {
        return `Koalas win (${avgKoalas} vs ${avgDolphins})`;
    } else return `No one won the game 😒`;
}

console.log(checkWinner(avgDolphins, avgKoalas));*/

/*const user = {
    firstName: 'John',
    lastName: 'Christian',
    isAdult: true,
    birthYear: 1998,
    // calcAge: function (birthYear) {
    //     return 2021 - birthYear;
    // },
    // calcAge: function () {
    //     return 2021 - this.birthYear;
    // },
    calcAge: function () {
        this.age = 2021-this.birthYear;
        return this.age;
    },

    calcProjects: years => years * 4,
}

// console.log(user.calcAge(1998));
console.log(user.calcAge());
console.log(user.age);
console.log(user.calcProjects(3));
console.log(user['calcProjects'](6));*/
